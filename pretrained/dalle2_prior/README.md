Place the downloaded LAION DALL-E 2 prior files here:

- `prior_config.json`
- `best.pth` (recommended) or `latest.pth`

Download both the configuration and the checkpoint separately; neither is bundled with the code.
